name: Deploy HTML to EC2

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy index.html to Apache
        run: |
          sudo cp index.html /var/www/html/index.html
          sudo systemctl restart apache2
